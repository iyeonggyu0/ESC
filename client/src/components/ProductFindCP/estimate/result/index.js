import ProductFindHeader from '../../_common/header';

const EstimateResult = ({ productData, selectionList }) => {
  return (
    <div>
      <ProductFindHeader Page={'result'} />
    </div>
  );
};
export default EstimateResult;
